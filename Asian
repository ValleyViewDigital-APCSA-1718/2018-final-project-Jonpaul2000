import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class SpriteDriver here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Asian extends Guy
{
    /**
     * Act - do whatever the SpriteDriver wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */

    public Asian(String name, int wide, int high)
    {
       super(name, wide, high);
    }
    public void act() 
    {
        // Add your action code here.
        move();
        jump();
        attack();
        block();
    }
}
